{% extends 'base.html.twig' %}

{% block title %} Evenement {% endblock %}

{% block body %}
    <h1 align="center"> Evenement </h1>
    <a href="{{ path('evenement_index') }}"class=" btn btn-primary">Go back</a>
    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ evenement.id }}</td>
        </tr>
        <tr>
            <th>Titre</th>
            <td>{{ evenement.titre }}</td>
        </tr>
        <tr>
            <th>Lieu</th>
            <td>{{ evenement.lieu }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ evenement.description }}</td>
        </tr>
        <tr>
            <th>Image</th>
            <td>{{ evenement.image }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ evenement.type }}</td>
        </tr>
        <tr>
            <th>Date</th>
            <td>{{ evenement.date ? evenement.date|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>Prix</th>
            <td>{{ evenement.prix }}</td>
        </tr>
        <tr>
            <th>Nombre de Participants</th>
            <td>{{ evenement.nbrmaxpart }}</td>
        </tr>
        <tr>
            <th>Etat</th>
            <td>{{ evenement.etat }}</td>
        </tr>
        </tbody>
    </table>



    <a href="{{ path('evenement_edit', {'id': evenement.id}) }} "class=" btn btn-primary" >edit</a>

    {{ include('evenement/_delete_form.html.twig') }}
{% endblock %}
