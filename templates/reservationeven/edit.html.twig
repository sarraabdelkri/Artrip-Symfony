{% extends 'base.html.twig' %}

{% block title %}Modifier  Reservation Evenement{% endblock %}

{% block body %}
    <h1>Modifier  la Reservation Evenement</h1>
    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
    {{ include('reservationeven/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('reservationeven_index') }}" class=" btn btn-primary" >GO Back</a>

    {{ include('reservationeven/_delete_form.html.twig') }}
{% endblock %}
